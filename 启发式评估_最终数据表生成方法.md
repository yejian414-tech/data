# 启发式评估最终数据表的生成方法



## 1. 收集评价者原始记录

三名评价者 E1、E2、E3 分别操作四个模块，并各自填写：

| Evaluator | Feature | 操作 | 发现的问题 | 违反原则 | Severity | Evidence |
|---|---|---|---|---|---:|---|
| E1 | Coach | 请求实时建议 | 未提示必须先启动 Coach | 1, 6 | 4 | Screenshot/Note |
| E2 | Coach | 请求实时建议 | 前置条件不可见 | 1, 6 | 3 | Screenshot/Note |
| E3 | Coach | 启动实时指导 | 不知道 Coach 当前未运行 | 1 | 4 | Screenshot/Note |

> 以上只是说明数据结构的示例；如果没有三名评价者的真实记录，不能把示例当成实验数据。

## 2. 判断哪些记录属于同一个问题

合并标准通常包括：

- 出现在相同页面或功能；
- 由相同设计缺陷导致；
- 对用户产生相同影响；
- 违反相同或相关的启发式原则。

上面三条记录的根本原因都是：

> Coach 的启动前置条件和当前状态没有显示清楚。

因此合并为一个问题，而不是计算成三个问题。

## 3. 统一问题描述

最终问题描述应包含：

```text
触发条件 + 界面问题 + 对用户的影响
```

例如：

```text
Without Coach switched on, users are not aware that they need to activate it to receive live guidance.
```

不要只写：

```text
Coach is confusing.
```

因为这种描述无法说明问题发生在哪里，以及为什么构成可用性问题。

## 4. 合并违反的原则

把三名评价者指出的所有相关原则合并并复核：

- 原则1：没有显示 Coach 当前是否运行；
- 原则6：用户必须记住先启动 Coach 这一前置步骤。

最终写为：

```text
1, 6
```

原则编号不是问卷分数，也不需要求平均。

## 5. 得到最终严重度

每名评价者先独立给出0–4分，再使用预先规定的规则汇总。比较稳妥的方法有两种。

### 方法A：讨论后达成共识

E1、E2、E3查看证据并讨论，最后共同确定一个整数严重度。这种方法适合形成论文表格中的整数结果。

### 方法B：取中位数

例如三名评价者给出：

```text
E1 = 4
E2 = 3
E3 = 4
Median = 4
```

最终严重度为4。

建议采用中位数，因为0–4严重度是有序等级，而不是精确的连续测量。如果最高分和最低分相差2分以上，应重新讨论原因并保存讨论记录。

## 6. 形成合并后的主表

原始表处理完成后，建立一个 merged findings 表：

| Finding ID | E1 | E2 | E3 | Final severity | Final issue | Heuristics |
|---|---:|---:|---:|---:|---|---|
| H1 | 4 | 3 | 4 | 4 | Coach prerequisite is not visible | 1, 6 |
| H2 | 4 | 4 | 3 | 4 | Bot Switch and Stop are unclear | 1, 2 |
| H3 | 3 | 3 | 2 | 3 | Coach modes are visually indistinct | 6, 8 |

然后删除用于分析的 E1–E3 列，只保留论文需要展示的三列：

| Severity | Issue | Heuristic violated |
|---:|---|---|
| 4 | Coach prerequisite is not visible | 1, 6 |
| 4 | Bot Switch and Stop are unclear | 1, 2 |
| 3 | Coach modes are visually indistinct | 6, 8 |

这就是论文 Table C.1 的生成方式。

## 7. 按严重度排序

最终表一般按照严重度从高到低排列：

1. Severity 4；
2. Severity 3；
3. Severity 2；
4. Severity 1。

同一严重度内可以按模块、发现顺序或修复优先级排序。

## 8. 生成第二轮严重度分布

修改界面后，评价者再次独立检查，重新生成一份第二轮问题表。然后按最终严重度计数：

```text
Severity 4 = 0
Severity 3 = 1
Severity 2 = 3
Severity 1 = 3
```

Excel 可以使用：

```excel
=COUNTIF(FinalSeverityRange,4)
=COUNTIF(FinalSeverityRange,3)
=COUNTIF(FinalSeverityRange,2)
=COUNTIF(FinalSeverityRange,1)
```

由此形成 C.5：

| Severity | Round 1 | Round 2 |
|---:|---:|---:|
| 4 | 2 | 0 |
| 3 | 2 | 1 |
| 2 | 2 | 3 |
| 1 | 1 | 3 |

## 9. 当前证据边界

当前论文只提供合并后的7项结果，没有 E1–E3 的真实独立评分。因此能够保留 C.1，但不能反向生成评价者原始评分。

如果原始表确实遗失，应标记为：

```text
[MISSING EVIDENCE: individual heuristic-evaluation records and severity ratings from E1–E3]
```

## 课程来源

`SE/lecture/7a HCI Evaluation Part One 2025.pdf`，PDF第19–46页。

